services:
  server:
    build: .
    image: ghcr.io/markredeman/mujoco-so101-experiment:latest
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    environment:
      - JULIA_NUM_THREADS=auto
      - PORT=${PORT:-8080}
    restart: unless-stopped
